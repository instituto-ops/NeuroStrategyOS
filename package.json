{
  "scripts": {
    "dev": "npm run dev --prefix neurostrategy-os",
    "tauri": "npm run tauri --prefix neurostrategy-os",
    "install-all": "npm install && npm install --prefix neurostrategy-os",
    "build": "npm run build --prefix neurostrategy-os"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@tiptap/core": "^3.14.0",
    "@tiptap/starter-kit": "^3.14.0"
  }
}
