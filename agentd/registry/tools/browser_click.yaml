id: "browser.click"
version: "1.0.0"
description: "Simulates a click on a page element using CSS selectors."
risk_level: "moderate"
requires_approval: true
input_schema:
  type: "object"
  properties:
    selector:
      type: "string"
      description: "CSS selector of the element to click."
  required: ["selector"]
applicable_skills: ["*"]
