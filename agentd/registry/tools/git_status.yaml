id: "git.status"
version: "1.0.0"
description: "Returns git status of the repository."
risk_level: "safe"
reversible: true
requires_approval: false
timeout_ms: 10000
input_schema:
  type: "object"
  properties:
    cwd:
      type: "string"
      description: "Repository root path."
  required: ["cwd"]
applicable_skills: ["*"]
