id: "documents.parse_docx"
version: "1.0.0"
description: "Extracts text from Word (DOCX) files."
risk_level: "safe"
requires_approval: false
input_schema:
  type: "object"
  properties:
    path:
      type: "string"
      description: "Absolute path to the DOCX file."
  required: ["path"]
applicable_skills: ["*"]
